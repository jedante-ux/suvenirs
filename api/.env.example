# Server Configuration
PORT=4000
NODE_ENV=development  # Change to 'production' for deployment

# Database - MongoDB Atlas
# Get your connection string from https://mongodb.com/atlas
# Format: mongodb+srv://<username>:<password>@<cluster>.mongodb.net/<database>
MONGODB_URI=mongodb://localhost:27017/suvenirs

# JWT Authentication
# IMPORTANT: Use a strong, unique secret in production (min 32 characters)
# Generate one with: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRES_IN=7d

# CORS - Allowed Origins
# For multiple origins, separate with commas: https://app.example.com,https://admin.example.com
# Use your Vercel deployment URL in production
CORS_ORIGIN=http://localhost:3000

# File Upload
MAX_FILE_SIZE=5242880
UPLOAD_DIR=uploads

# Optional: Pexels API for stock images
# PEXELS_API_KEY=your-pexels-api-key
